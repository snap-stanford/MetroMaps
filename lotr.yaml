input_helper:
    mode: on
    name: whitelistcounter
    whitelist: domains/lotr/data/whitelist.txt
    input_directory: domains/lotr/data/rawtext
    #docs_metadata: domains/lotr/data/doc_meta.json

legacy_helper:
    mode: on
    doc_metadata: domains/lotr/data/doc_meta.json
    num_timeslices: 20
    

clustering:
    mode: on
    similarity_merge: .95 # <intersection> / <small size> above this limit merges
    dilution_merge: .1
    

mapgen:
    mode: on
    chosen_lines: lotr.mm
    chosen_lines_json: lotr.json

vizbuilder:
    mode: off
    name: clusterdescription
    input_lines: lotr.mm
    final_map_json: lotr.json

